graph LR
    classDef s fill:#2980b9,stroke:#fff,stroke-width:2px,color:#fff;
    classDef m fill:#3498db,stroke:#fff,stroke-width:2px,color:#fff;
    classDef r fill:#16a085,stroke:#fff,stroke-width:2px,color:#fff;
    classDef so fill:#e67e22,stroke:#fff,stroke-width:2px,color:#fff;
    classDef c fill:#8e44ad,stroke:#fff,stroke-width:2px,color:#fff;
    classDef i fill:#7f8c8d,stroke:#fff,stroke-width:2px,color:#fff;

    %% Ядро и Менеджеры
    IR[InputReader]:::i
    AM[AnxietyManager]:::s
    AuM[AudioManager]:::s
    GLM[GameLoopManager]:::s
    TM[TimeManager]:::s
    PM[PauseMenu]:::m
    SM[SettingsManager]:::m
    MMM[MainMenuManager]:::m

    %% Данные
    SP[SessionProgress]:::i
    SD[SoundData]:::so
    CID[ClothingItemData]:::so
    TD[ThoughtData]:::so
    SS[SceneStep]:::i

    %% Игрок и Нарратив
    FPC[FirstPersonController]:::i
    DTC[DeathTransitionController]:::c
    CM[CutsceneManager]:::c
    CT[CutsceneTrigger]:::c
    TT[ThoughtTrigger]:::c
    CC[CinematicController]:::s
    SubM[SubtitleManager]:::s

    %% Интерфейс и ритуалы в ряд
    IRC[IRitualController<br/>Interface]:::i
    CRC[CucumberRitual<br/>Controller]:::r
    ClRC[ClosetRitual<br/>Controller]:::r
    DZR[Dishwashing<br/>Ritual]:::r
    PWR[PlantWatering<br/>Ritual]:::r

    %% Компоненты ритуалов
    RA[RitualActivator]:::i
    RCH[RitualCameraHandler]:::i
    AAE[AmbientAudioEmitter]:::i
    CBC[CucumberBoardController]:::r
    SDC[SaladDeliveryController]:::r
    CI[ClothingItem]:::r
    BCC[BathroomColorsController]:::r
    SMG[SoilMeshGenerator]:::r

    %% === НАСЛЕДОВАНИЕ: ряд ритуалов под интерфейсом ===
    IRC --- CRC
    IRC --- ClRC
    IRC --- DZR
    IRC --- PWR

    %% === СВЯЗИ: Ввод ===
    IR -->|Gameplay| FPC
    IR -->|UI| PM
    IR -->|Ritual| CRC
    IR -->|Ritual| ClRC
    IR -->|Ritual| DZR
    IR -->|Ritual| PWR
    RA -->|Activate| IRC
    RCH -->|Block| FPC

    %% === СВЯЗИ: Системное меню ===
    PM -->|Check| IRC
    PM --> SM
    MMM --> SM

    %% === СВЯЗИ: Цикл тревожности ===
    TM -->|Time| AM
    AM -->|Threshold| AuM
    AM -->|Tremor| CRC
    AM -->|Tremor| DZR
    AM -->|Tremor| PWR
    AM -->|Tier| ClRC
    CRC -->|Penalty| AM
    DZR -->|Penalty| AM
    PWR -->|Penalty| AM
    ClRC -->|Penalty| AM

    %% === СВЯЗИ: Жизненный цикл ===
    AM -->|Breakdown| GLM
    CRC -->|Complete| GLM
    ClRC -->|Complete| GLM
    DZR -->|Complete| GLM
    SDC -->|Complete| GLM
    GLM -->|Death| DTC
    GLM -->|Death| CRC
    GLM -->|Death| ClRC
    GLM -->|Death| DZR
    GLM -->|Death| PWR
    GLM -->|Reset| IRC

    %% === СВЯЗИ: Смерть и катсцены ===
    DTC -->|Ragdoll| FPC
    DTC -->|Loop| GLM
    DTC -->|Cutscene| CM
    CT -->|Trigger| CM
    CM -->|Control| CC
    CM -->|Steps| SS
    CC -->|Lock| FPC

    %% === СВЯЗИ: Прогресс ===
    GLM -->|Count| SP
    CBC -->|Save| SP
    ClRC -->|Save| SP
    BCC -->|Read| SP

    %% === СВЯЗИ: Внутренние механики ритуалов ===
    ClRC -->|Sync| BCC
    CI -->|Paint| ClRC
    CRC -->|Slice| CBC
    CRC -->|Spawn| SDC
    SDC -->|Aim| RA
    PWR -->|Mesh| SMG

    %% === СВЯЗИ: Субтитры ===
    TT -->|Show| SubM
    CM -->|Show| SubM
    PWR -->|Show| SubM
    ClRC -->|Show| SubM

    %% === СВЯЗИ: Scriptable Objects ===
    SD -.-> AuM
    SD -.-> FPC
    SD -.-> AAE
    SD -.-> MMM
    SD -.-> CRC
    SD -.-> ClRC
    SD -.-> DZR
    SD -.-> PWR
    SD -.-> SDC
    SD -.-> DTC
    SD -.-> GLM
    TD -.-> SubM
    TD -.-> TT
    TD -.-> SS
    TD -.-> SD
    CID -.-> CI