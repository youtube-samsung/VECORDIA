flowchart TD
    %% ==========================================
    %% СЛОЙ 1: ИНСТРУМЕНТЫ И ДАННЫЕ (Tools & Data)
    %% ==========================================
    subgraph Tools [ИНСТРУМЕНТЫ И ДАННЫЕ]
        InputReader["InputReader\n(Чтение и раздача ввода)"]:::blue
        
        subgraph AudioTool [Аудиосистема]
            AudioManager["AudioManager\n(Воспроизведение звуков)"]:::blue
            SoundData["SoundData\n(ScriptableObject)"]:::orange
        end
        
        subgraph TextTool [Текст и UI]
            SubtitleManager["SubtitleManager\n(Вывод субтитров на экран)"]:::blue
            ThoughtData["ThoughtData\n(ScriptableObject)"]:::orange
            ChoiceManager["ChoiceManager\n(Интерфейс QTE-выбора)"]:::blue
        end
        
        CinematicController["CinematicController\n(Управление виртуальными камерами)"]:::blue
    end

    %% ==========================================
    %% СЛОЙ 2: СОСТОЯНИЕ И СЮЖЕТ (Meta & Narrative)
    %% ==========================================
    subgraph StateFlow [СОСТОЯНИЕ И СЮЖЕТ]
        GameFlowManager["GameFlowManager\n(Мозг: отслеживает фазы, меняет мир)"]:::purple
        AnxietyManager["AnxietyManager\n(Счетчик паранойи)"]:::purple
        
        subgraph Cutscenes [Катсцены]
            CutsceneManager["CutsceneManager\n(Плеер событий)"]:::purple
            SceneStep["SceneStep\n(Data: шаги анимации)"]:::orange
        end
    end

    %% ==========================================
    %% СЛОЙ 3: СИСТЕМА РИТУАЛОВ (Gameplay)
    %% ==========================================
    subgraph Rituals [СИСТЕМА РИТУАЛОВ]
        RitualDirector["RitualDirector\n(Режиссер прерываний)"]:::redDirector
        IRitualController["IRitualController\n(Интерфейс)"]:::redInterface
        RitualCameraHandler["RitualCameraHandler\n(Локальная камера ритуала)"]:::red
        
        CucumberRitual["CucumberRitual"]:::redImpl
        DishwashingZone["DishwashingZone"]:::redImpl
        PlantWatering["PlantWatering"]:::redImpl
        ClosetRitual["ClosetRitual"]:::redImpl
        
        ClothingItemData["ClothingItemData\n(ScriptableObject)"]:::orange
        SoilMeshGenerator["SoilMeshGenerator\n(Визуал земли)"]:::orange
    end

    %% ==========================================
    %% СЛОЙ 4: ИГРОВОЙ МИР (World Interactions)
    %% ==========================================
    subgraph World [ИГРОВОЙ МИР]
        FirstPersonController["FirstPersonController\n(Игрок)"]:::green
        RitualActivator["RitualActivator\n(Триггер у стола/шкафа)"]:::green
        UniversalDistraction["UniversalDistraction\n(Кран, ТВ - отвлекающие факторы)"]:::green
        ThoughtTrigger["ThoughtTrigger\n(Невидимая зона для мыслей)"]:::green
        EndGameInteractables["EndGameInteractables\n(Рамка, Ванна, Нож)"]:::green
    end

    %% ==========================================
    %% СВЯЗИ И ВЗАИМОДЕЙСТВИЯ
    %% ==========================================

    %% Ввод
    InputReader -->|WASD, Мышь| FirstPersonController
    InputReader -->|E, ЛКМ| RitualActivator
    InputReader -->|E, ЛКМ| ChoiceManager
    InputReader -->|E, ЛКМ| UniversalDistraction
    InputReader -->|E, ЛКМ| EndGameInteractables
    InputReader -->|E, ЛКМ| IRitualController

    %% Связь Инструментов и Данных
    SoundData -.->|Читается| AudioManager
    ThoughtData -.->|Читается| SubtitleManager
    ClothingItemData -.->|Читается| ClosetRitual

    %% Катсцены и Инструменты
    CutsceneManager -->|Читает настройки| SceneStep
    CutsceneManager -->|Командует камерой| CinematicController
    CutsceneManager -->|Командует звуком| AudioManager

    %% Ритуалы и Инструменты (Геймплей использует сервисы)
    IRitualController -->|Вызов звуков| AudioManager
    RitualCameraHandler -->|Переключает Action Map| InputReader

    %% Логика старта ритуалов
    RitualActivator -->|Команда StartRitual| IRitualController
    IRitualController -->|Включает зум| RitualCameraHandler
    
    IRitualController -.->|Реализация| CucumberRitual
    IRitualController -.->|Реализация| DishwashingZone
    IRitualController -.->|Реализация| PlantWatering
    IRitualController -.->|Реализация| ClosetRitual
    
    PlantWatering -->|Обновляет| SoilMeshGenerator

    %% Взаимодействие с AnxietyManager (В обе стороны)
    CucumberRitual -->|Ошибки повышают тревогу| AnxietyManager
    DishwashingZone -->|Ошибки повышают тревогу| AnxietyManager
    AnxietyManager -->|Влияет на тремор ножа| CucumberRitual
    AnxietyManager -->|Влияет на громкость сердца| AudioManager

    %% Режиссура
    CucumberRitual -->|Событие OnInterruptionRequested| RitualDirector
    DishwashingZone -->|Событие OnInterruptionRequested| RitualDirector
    RitualDirector -->|Выключает скрипт| IRitualController
    RitualDirector -->|Командует TurnOn| UniversalDistraction
    RitualDirector -->|Запускает QTE| ChoiceManager

    %% Мир и Сюжет
    UniversalDistraction -->|Играет 3D шум| AudioManager
    UniversalDistraction -->|Сообщает о починке| GameFlowManager
    ThoughtTrigger -->|Передает ID мысли| SubtitleManager
    
    GameFlowManager -->|Блокирует двери/меняет текстуры| EndGameInteractables
    EndGameInteractables -->|Игрок сделал финальный выбор| GameFlowManager
    GameFlowManager -->|Запускает нужный финал| CutsceneManager

    %% Стили узлов
    classDef blue fill:#2a4a9c,stroke:#fff,stroke-width:2px,color:#fff;
    classDef purple fill:#6b3e9e,stroke:#fff,stroke-width:2px,color:#fff;
    classDef green fill:#4f9c54,stroke:#fff,stroke-width:2px,color:#fff;
    classDef red fill:#b73e34,stroke:#fff,stroke-width:2px,color:#fff;
    classDef redInterface fill:#b73e34,stroke:#fff,stroke-width:2px,stroke-dasharray: 5 5,color:#fff;
    classDef redImpl fill:#d95a4f,stroke:#fff,stroke-width:1px,color:#fff;
    classDef redDirector fill:#d95a4f,stroke:#fff,stroke-width:3px,color:#000;
    classDef orange fill:#df9b3e,stroke:#fff,stroke-width:1px,color:#fff;