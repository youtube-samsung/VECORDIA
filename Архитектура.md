```mermaid
flowchart TD

    subgraph Tools [ИНСТРУМЕНТЫ И ДАННЫЕ]
        InputReader["InputReader(Чтение и раздача ввода)"]:::blue
        
        subgraph AudioTool [Аудиосистема]
            AudioManager["AudioManager\n(Менеджер звуков)"]:::blue
            SoundData["SoundData(ScriptableObject)"]:::orange
        end
        
        subgraph TextTool [Текст и UI]
            SubtitleManager["SubtitleManager(Вывод субтитров на экран)"]:::blue
            ThoughtData["ThoughtData(ScriptableObject)"]:::orange
            ChoiceManager["ChoiceManager\n(Вывод панели выбора в ритуале на экран)"]:::blue
        end
        
        CinematicController["CinematicController(Набор корутин для управления камерой)"]:::blue
    end


    subgraph StateFlow [СОСТОЯНИЕ И СЮЖЕТ]
        GameFlowManager["GameFlowManager\n(Управление процессом игры, следование сюжету)"]:::purple
        AnxietyManager["AnxietyManager\n(глобальный счетчик тревожности)"]:::purple
        
        subgraph Cutscenes [Катсцены]
            CutsceneManager["CutsceneManager\n(Универсальное управление катсценами и осмотром предметов)"]:::purple
            SceneStep["SceneStep(Структура данных, кусочки для Cutscenemanger)"]:::orange
        end
    end


    subgraph Rituals [СИСТЕМА РИТУАЛОВ]
        RitualDirector["RitualDirector(Управление прерываниями в ритуалах)"]:::redDirector
        IRitualController["IRitualController(Интерфейс для всех ритуалов)"]:::redInterface
        RitualCameraHandler["RitualCameraHandler(Переключение в режим ритуала)"]:::red
        
        CucumberRitual["CucumberRitual"]:::redImpl
        DishwashingZone["DishwashingRitual"]:::redImpl
        PlantWatering["PlantWateringRitual"]:::redImpl
        ClosetRitual["ClosetRitual"]:::redImpl
        
        ClothingItemData["ClothingItemData(Данные цвета и индекса для футболок в шкафу)"]:::orange
        SoilMeshGenerator["SoilMeshGenerator(Генерация меша для ритуала с поливом)"]:::orange
    end

    subgraph World [ИГРОВОЙ МИР]
        FirstPersonController["FirstPersonController(Передвижение игрока по карте от первого лица)"]:::green
        RitualActivator["RitualActivator(Триггер, для запуска ритуалов)"]:::green
        UniversalDistraction["UniversalDistraction\n(Скрипт для отвлекающих факторов)"]:::green
        ThoughtTrigger["ThoughtTrigger\n(Триггер для субтитров)"]:::green
        EndGameInteractables["EndGameInteractables\n(Скрипт для предметов, ведущих к концовкам)"]:::green
    end


    %% Ввод
    InputReader -->|WASD, Мышь| FirstPersonController
    InputReader -->|E| RitualActivator
    InputReader -->|E| ChoiceManager
    InputReader -->|E| UniversalDistraction
    InputReader -->|E| EndGameInteractables
    InputReader -->|ЛКМ| IRitualController

    %% Связь Инструментов и Данных
    SoundData -.->|Читается| AudioManager
    ThoughtData -.->|Читается| SubtitleManager
    ClothingItemData -.->|Читается| ClosetRitual

    %% Катсцены и Инструменты
    CutsceneManager -->|Читает настройки для каждого шага| SceneStep
    CutsceneManager -->|Использует камерой| CinematicController
    CutsceneManager -->|Использует звуком| AudioManager
    CutsceneManager -->|Использует текст| SubtitleManager

    %% Ритуалы и Инструменты (Геймплей использует сервисы)
    IRitualController -->|использует| AudioManager
    RitualCameraHandler -->|Переключает Action Map| InputReader


    
    IRitualController -.->|Реализация| CucumberRitual
    IRitualController -.->|Реализация| DishwashingZone
    IRitualController -.->|Реализация| PlantWatering
    IRitualController -.->|Реализация| ClosetRitual
    
    PlantWatering -->|Обновляет| SoilMeshGenerator

    %% Взаимодействие с AnxietyManager (В обе стороны)
    IRitualController -->|Ошибки повышают тревогу| AnxietyManager
    
    AnxietyManager -->|Влияет на сложность выполнения| IRitualController
    AnxietyManager -->|Влияет на эмбиент| AudioManager

    %% Режиссура
    IRitualController -->|Событие OnInterruptionRequested| RitualDirector
    RitualDirector -->|Выключает скрипт ритуала(паузит)| IRitualController
    RitualDirector -->|Включает отвлекающий фактор| UniversalDistraction
    RitualDirector -->|Включает выбор - ChoiceManager| ChoiceManager

    %% Мир и Сюжет
    UniversalDistraction -->|Отвл. фактор использует 3д звук| AudioManager
    UniversalDistraction -->|Сообщает о взаимодействии с отвл. фактором геймменеджеру| GameFlowManager
    ThoughtTrigger -->|Включает мысль при входе в тригер зону| SubtitleManager
    
    GameFlowManager -->|Блокирует зоны для концовок| EndGameInteractables
    EndGameInteractables -->|Игрок сделал финальный выбор( из доступных ему)| GameFlowManager
    GameFlowManager -->|Запускает нужную катсцену финала| CutsceneManager

    %% Стили узлов
    classDef blue fill:#2a4a9c,stroke:#fff,stroke-width:2px,color:#fff;
    classDef purple fill:#6b3e9e,stroke:#fff,stroke-width:2px,color:#fff;
    classDef green fill:#4f9c54,stroke:#fff,stroke-width:2px,color:#fff;
    classDef red fill:#b73e34,stroke:#fff,stroke-width:2px,color:#fff;
    classDef redInterface fill:#b73e34,stroke:#fff,stroke-width:2px,stroke-dasharray: 5 5,color:#fff;
    classDef redImpl fill:#d95a4f,stroke:#fff,stroke-width:1px,color:#fff;
    classDef redDirector fill:#d95a4f,stroke:#fff,stroke-width:3px,color:#000;
    classDef orange fill:#df9b3e,stroke:#fff,stroke-width:1px,color:#fff;
